{
  "type": "postgres",
  "host": "172.18.0.3",
  "port": 5432,
  "username": "typeorm",
  "password": "typeorm",
  "database": "dogs",
  "synchronize": true,
  "logging": false,
  "entities": ["src/**/*.entity.ts"],
  "migrations": ["src/migration/**/*.ts"],
  "default": true,
  "cli": {
    "migrationsDir": "src/migration"
  }
}
